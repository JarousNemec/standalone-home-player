fastapi>=0.110
uvicorn[standard]>=0.29
# 1.12.2 rozbíjí filtrované hledání (filter=songs/albums/artists/videos
# vrací 0 výsledků, playlists funguje). Držíme poslední funkční verzi;
# před odepnutím ověř `search(..., filter='songs')`.
ytmusicapi==1.12.1
yt-dlp[default,deno]>=2026.7.4
python-mpv>=1.0.6
# přímo používaný v app/session.py (rotace cookies); jinak tranzitivně z ytmusicapi
requests>=2.31
pydantic>=2.6
